# inode and rm

## Why rm is dangerous

- Linux has no recycle bin
- rm removes the reference to inode
- Always backup before deleting files

- uptime gives quick load overview
- free -h shows memory pressure
- swap usage is a warning sign
